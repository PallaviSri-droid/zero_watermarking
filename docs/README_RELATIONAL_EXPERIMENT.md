# Relational experiment

The relational patch identity residual is experimental. The frozen CAP-DINO-LogPolar stable-repair checkpoint remains the baseline. Promotion requires common 200-image evaluation showing improved collision-tail behavior without unacceptable robustness regression.